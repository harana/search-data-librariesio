<li class="prevent-select relative flex justify-between gap-x-6 px-4 py-5 hover:bg-gray-50 sm:px-6">
  <div class="flex min-w-0 gap-x-4">
    <div class="min-w-0 flex-auto">
      <a href="<%= project.url %>">
        <p class="text-lg font-semibold leading-6 text-gray-900">
          <span class="absolute inset-x-0 -top-px bottom-0"></span>
          <%= project.name %>
        </p>
        <p class="mt-1 flex text-sm leading-5 text-gray-500">
            <%= project.description %>
        </p>
      </a>
    </div>
  </div>
  <div class="flex shrink-0 items-center gap-x-4">
    <div class="hidden sm:flex sm:flex-col sm:items-end">
      <a href="<%= project.url %>">
        <p class="text-sm leading-6 text-gray-700"><%= project.latest_stable_release_number.to_s.gsub("+incompatible", "") %></p>
      </a>
    </div>
    <a href="<%= project.url %>">        
      <svg class="h-5 w-5 flex-none text-gray-400" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
        <path fill-rule="evenodd" d="M7.21 14.77a.75.75 0 01.02-1.06L11.168 10 7.23 6.29a.75.75 0 111.04-1.08l4.5 4.25a.75.75 0 010 1.08l-4.5 4.25a.75.75 0 01-1.06-.02z" clip-rule="evenodd" />
      </svg>
    </a>
  </div>
</li>